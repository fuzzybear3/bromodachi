#!/usr/bin/env bash
# bromodachi v2: keybinds and autostart point at this path; the program is
# the Rust binary. Build with: cargo build --release
exec "$(dirname "$0")/target/release/bromodachi" "$@"
